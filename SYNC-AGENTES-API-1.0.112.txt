SYNC API — sessão persistente (hotfix Railway)
==============================================
Status: ✅ PRONTO

Zona: API / Segurança
Causa: apply_user_auth ainda fazia GoTrue set_session em cada pedido
       autenticado → refresh do telemóvel morria → senha de manhã.

Ficheiros:
- ego_api/supabase_client.py — só postgrest.auth(JWT)
- flask_api.py — api_build 2026-08-17-auth-no-set-session-rls
- scripts/regression_guard.py — bloqueia regressão

Sem build EAS. Depois do deploy: login UMA vez; deve ficar logado.
