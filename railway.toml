# EGO-AI — Streamlit (app.py). API Flask fica em Dockerfile.api (outro serviço).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.streamlit"

[deploy]
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
