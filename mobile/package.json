{
  "name": "ego-ai-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "App Android e iOS (Capacitor) para o EGO-AI publicado em HTTPS",
  "scripts": {
    "postinstall": "node scripts/patch-android-manifest.js || exit 0",
    "configure": "node scripts/configure.js",
    "cap:sync": "npx cap sync",
    "cap:copy": "npx cap copy",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "init:android": "npx cap add android",
    "init:ios": "npx cap add ios",
    "android": "npm run cap:sync && npm run cap:open:android",
    "ios": "npm run cap:sync && npm run cap:open:ios",
    "build:android": "npm run cap:sync && cd android && (if exist gradlew.bat (gradlew.bat assembleDebug) else ./gradlew assembleDebug)"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
