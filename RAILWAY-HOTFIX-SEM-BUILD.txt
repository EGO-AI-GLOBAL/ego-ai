# Hotfix API — SEM nova build do app (só deploy Railway)
# https://railway.app → serviço API → Redeploy após git push

# === CHAT ANDROID TESTADORES (Jul 2026) ===
# Se «Não foi possível concluir» ou chat não envia no Android:
EGO_PLAY_INTEGRITY=1
EGO_PLAY_INTEGRITY_MODE=monitor
# enforce só depois que todos usam Play teste interno + build 1.0.78+

# Confirmar (não mudar para enforce enquanto testa voz):
EGO_MAINTENANCE=0

# Já devem estar (1.0.60):
EGO_LATEST_APP_VERSION=1.0.60
EGO_LATEST_ANDROID_VERSION_CODE=99

# Depois do deploy, /health deve mostrar:
# api_build=2026-06-29-1.0.60-hotfix-voz-phone

# O que este deploy corrige (app 1.0.60 já instalado):
# 1. Telefone no /me — para de pedir «Complete seu cadastro» em loop
# 2. Voz Android — auth tolera refresh expirado no upload nativo
# 3. Voz — lê áudio multipart com nomes alternativos de campo
# 4. Play Integrity — só exige token com X-EGO-Platform: android explícito
