# Railway — Variables — 1.0.85 (após build nas lojas)
# Painel: https://railway.app → ego-ai-production → Variables
# Deploy automático após git push main (~2 min)

# ========== CRÍTICAS (manter valores actuais) ==========
EGO_MAINTENANCE=0
SUPABASE_URL=(manter)
SUPABASE_KEY=(manter)
SUPABASE_SERVICE_ROLE_KEY=(manter)
GOOGLE_API_KEY=(manter)
OPENAI_API_KEY=(manter)

# ========== BANNER ATUALIZAÇÃO — mudar SÓ quando 1.0.85 estiver nas lojas ==========
EGO_LATEST_APP_VERSION=1.0.85
EGO_LATEST_ANDROID_VERSION_CODE=129
EGO_APP_UPDATE_MESSAGE=1.0.85: fica logado ao abrir pelo push + mais estabilidade.

# Resto: copiar de RAILWAY-VARS-1.0.84.txt (PAUSA, Stripe, Gemini, etc.)
