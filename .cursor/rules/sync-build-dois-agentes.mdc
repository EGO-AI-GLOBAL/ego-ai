---
description: Vários agentes em paralelo — um único build iOS+Android por release
alwaysApply: true
---

# Sync build — EGO-AI (3 agentes)

## Regra de ouro

**Nunca** gerar build nem submeter loja sozinho se outros agentes também estão a trabalhar.

| Fazer | Não fazer |
|-------|-----------|
| `commit + push` → esperar **todos** → **um** `GERAR-E-SUBMETER-JUNTO.bat` | `GERAR-1.0.*.bat` só Android ou só iOS |
| `AGUARDAR-E-SUBMETER.bat` depois dos dois FINISHED | `SUBMIT-IOS-*.bat` / `PUBLICAR-*-PLAY.bat` separados |
| `python scripts/wait_and_submit_eas.py sync-check` antes do build | `eas submit --latest` |

## Fluxo obrigatório

1. Terminar código → `git pull origin main` → `commit` → `push`
2. Avisar o utilizador: **"pronto para sync build"** — esperar os **outros 2 agentes**
3. `python scripts/wait_and_submit_eas.py sync-check` (sem diff pendente em `app/` e `ego_api/`)
4. `GERAR-E-SUBMETER-JUNTO.bat` (iOS + Android na mesma versão `app.config.ts`)
5. Quando FINISHED: `AGUARDAR-E-SUBMETER.bat`

## Estado actual

**1.0.43 já submetido** — não gerar build de novo. Só `git pull origin main`.

Próximo build: quando os **3 agentes** tiverem push + `sync-check` OK.

## Zonas

- **A** — EGO de Bolso
- **B** — Monstrinhos
- **C** — Segurança/API

Detalhe: `SYNC-AGENTES-RELEASE.txt`
