/**
 * REMOVIDO (2026-08-26) — COMANDO-EGO-ADMOB-RECEITA-MAX-USO
 * Zero botão “vê anúncio”. Receita = banner + interstitial a cada 2 sessões fechadas.
 * Mantido como stub para não quebrar imports antigos.
 */
import type { AppColors } from "@/theme/colors";

type Props = {
  colors: AppColors;
  enabled: boolean;
  visible: boolean;
};

export function RewardedDailyTipCard(_props: Props): null {
  return null;
}
